/* WAP to accept an input "This is data" and output
 * sihT si atad */

#include <stdio.h>

#define NUMBER_OF_BYTES 100

int main(void) {
	int i, j, count;
	char buffer[NUMBER_OF_BYTES];
	printf("Enter the string: ");
	fgets(buffer, NUMBER_OF_BYTES, stdin);

	/* reverse the words */
	while(buffer[j] != 0) {
		for(i = 0;count ; i++, j++) {

		}
	}
	return 0;
}
